<%= form_with model: @discount, url: "/discounts/#{@discount.id}", local: true, method: :patch do |f| %>

  <%= f.label :item_threshold %>
  <%= f.number_field :item_threshold %>

  <%= f.label :value %>
  <%= f.number_field :value %>

  <%= f.submit('Update Discount') %>
<% end %>
